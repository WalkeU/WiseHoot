<div class="max-w-2xl mx-auto mt-10 mb-10 bg-white rounded-2xl shadow-lg p-8 border border-gray-100">
  <h1 class="text-2xl font-bold mb-6 text-gray-900">Account Settings</h1>
  
  <div class="mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-900">Your Profile</h2>
    <div class="p-4 bg-gray-50 rounded-xl">
      <div class="mb-2">
        <span class="font-semibold">Username:</span> <%= @user.username %>
      </div>
      <div class="mb-2">
        <span class="font-semibold">Email:</span> <%= @user.email.presence || "No email set" %>
      </div>
      <div class="mb-2">
        <span class="font-semibold">Account created:</span> <%= @user.created_at.strftime("%B %d, %Y") %>
      </div>
    </div>
  </div>
  
  <div class="border-t border-gray-200 pt-8 mt-8">
    <h2 class="text-xl font-semibold mb-4 text-red-600">Danger Zone</h2>
    <div class="p-4 bg-red-50 rounded-xl border border-red-200">
      <h3 class="font-semibold mb-2 text-red-700">Delete Account</h3>
      <p class="text-gray-700 mb-4">Once you delete your account, there is no going back. All your tweets and likes will be permanently removed.</p>
      
      <button onclick="confirmDelete()" class="px-4 py-2 bg-red-600 text-white rounded-xl hover:bg-red-700 font-semibold transition">Delete My Account</button>
      
      <div id="delete-confirmation" class="hidden mt-4 p-3 bg-red-100 rounded-xl">
        <p class="text-red-700 mb-2 font-semibold">Are you sure you want to delete your account?</p>
        <div class="flex space-x-2">
          <%= button_to "Yes, Delete My Account", destroy_account_path, method: :delete, class: "px-4 py-2 bg-red-600 text-white rounded-xl hover:bg-red-700 font-semibold transition" %>
          <button onclick="cancelDelete()" class="px-4 py-2 bg-gray-200 text-gray-700 rounded-xl hover:bg-gray-300 font-semibold transition">Cancel</button>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  function confirmDelete() {
    document.getElementById('delete-confirmation').classList.remove('hidden');
  }
  
  function cancelDelete() {
    document.getElementById('delete-confirmation').classList.add('hidden');
  }
</script>
